[user]
    name = nscuro
    email = nscuro@protonmail.com
    signingkey = 985ADB40F06304B3

[commit]
    gpgsign = true

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

[pull]
	ff = only
